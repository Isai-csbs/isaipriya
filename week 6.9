def find_index_mapping(A, B):
    mapping = {}
    for i, num in enumerate(B):
        mapping[num] = i
    
    return [mapping[num] for num in A]


n = int(input())
A = list(map(int, input().split()))


B = list(map(int, input().split()))


print(*find_index_mapping(A, B))
